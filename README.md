# CodeToNote

As can be understood from the name, this is a `.c` code to `.musicxml` music notation translator.

You can just compile the `composer.c` file and use it with given instructions. App takes one `.c` file and produces `.musicxml` in the same directory.

You can configure the composer from `composer.h` file. Which keyword / action corresponds which result etc.
